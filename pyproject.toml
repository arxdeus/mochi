[project]
name = "mochi"
version = "0.1.0"
description = "Pure Mojo port of the Maki coding agent"
readme = "README.md"
authors = [
    { name = "Artemis Kushner", email = "hello@arxdeus.dev" }
]
requires-python = ">=3.14"
dependencies = [
    "mojo>=1.0.0",
]
